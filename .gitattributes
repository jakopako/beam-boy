# Cartridge scripts and store metadata are hashed byte-for-byte (see
# docs/phase-7-store.md). Never let Git normalize their line endings, or the
# published bytes will silently drift from what's committed and break store
# hash verification on-device.
*.be -text
docs/games/**/meta.json -text
beam-boy-hw/data/games/**/meta.json -text
